---
title: sort() method
---

In [[python]], `sort()` is [[python-method]] to arrange a [[python-list]] in
ascending order.

```python
scores = [3, 6, 1, 12]
scores.sort()
print(scores)
>>> [1, 3, 6, 12]
```
